" Load plugins (NERDTree, Vim-fugitive, etc.)
source ~/.config/nvim/plugins.vim

" Load settings (tabspaces, colorscheme, etc.)
source ~/.config/nvim/settings.vim

" Load keybindings (jk to exit, ctrl-n to open NERDTree, etc.)
source ~/.config/nvim/keybindings.vim
